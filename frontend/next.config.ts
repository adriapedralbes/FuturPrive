import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com'], // Dominios permitidos para las imágenes
  },
};

export default nextConfig;